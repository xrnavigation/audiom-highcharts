{
  "name": "audiom-highcharts-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for the audiom-highcharts plugin and its sample harness.",
  "workspaces": [
    "sample",
    "plugin"
  ],
  "scripts": {
    "dev:sample": "npm run dev --workspace sample",
    "build:plugin": "npm run build --workspace @xrnavigation/audiom-highcharts",
    "test:plugin": "npm run test --workspace @xrnavigation/audiom-highcharts",
    "test:coverage": "npm run test:coverage --workspace @xrnavigation/audiom-highcharts",
    "test:ci": "npm run test:ci --workspace @xrnavigation/audiom-highcharts",
    "prebuild:sample": "node scripts/prebuild-sample-assets.mjs",
    "build:sample": "npm run prebuild:sample && npm run build:plugin && npm run build --workspace sample && node -e \"require('fs').writeFileSync('docs/.nojekyll','')\""
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
